services:
  mongo:
    image: mongo:8.3.8
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - ecommerce_native_mongo:/data/db

volumes:
  ecommerce_native_mongo:
